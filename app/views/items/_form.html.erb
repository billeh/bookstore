<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br />
    <%= f.select :book_id, Book.all.collect{|book| [book.name, book.id]} %>
  </div>
  <div class="field">
    <%= f.label :wishlist_id %><br />
    <%= f.select :wishlist_id, Wishlist.all.collect{|wishlist| [wishlist.name, wishlist.id]} %>
  </div>
  <div class="field">
    <%= f.label :quantity_ordered %><br />
    <%= f.number_field :quantity_ordered %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
